    if column_name in df.columns:
        # Construct the new column name
        new_column_name = f"level{number+1:03d}_Weight"
        # Rename the column
        df.rename(columns={column_name: new_column_name}, inplace=True)
